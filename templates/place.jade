extends layout

block body
  body
    include in/header
    div.place
      a(href= website)
        img.grav(src= image)
        h2= name
      p= about
      p= address + ' ' + city
      a.delete(href= '/places/delete/' + slug) Delete this place

    include in/footer
